import { Column } from "../../../../../../components";

export function DrawingCardsStep() {
  return (
    <Column>
      <p>
        Each player starts with a hand of three cards. As they play cards,
        whenever their hand is empty, three more cards are drawn from the deck.
      </p>
    </Column>
  );
}
